# ────────────────────────────────────────────────
# VARIABLES D'ENVIRONNEMENT — Les Poulettes Front
# Copiez ce fichier en .env.local et remplissez les valeurs
# Ne jamais committer .env.local ou .env.production
# ────────────────────────────────────────────────

# URL de l'API Strapi (backend)
VITE_API_URL=https://votre-api.onrender.com
API_URL=https://votre-api.onrender.com

# Google Analytics 4 — Measurement ID (ex: G-XXXXXXXXXX)
# Créez une propriété GA4 sur https://analytics.google.com
GA_MEASUREMENT_ID=

# Stripe — Clé publique (côté client, commence par pk_)
VITE_STRIPE_PUBLISHABLE_KEY=pk_live_...

# Stripe — Clé secrète (côté serveur UNIQUEMENT, ne jamais exposer côté client)
STRIPE_SECRET_KEY=sk_live_...

# Stripe — Clé webhook (pour les confirmations de paiement)
STRIPE_WEBHOOK_SECRET=whsec_...

# Resend — Email transactionnel (confirmations de commande, etc.)
RESEND_API_KEY=re_...

# URL du site en production
SITE_URL=https://lespoulettes.be

# ────────────────────────────────────────────────
# ENDPOINTS STRAPI À CRÉER (si pas encore fait)
# ────────────────────────────────────────────────
# - Collection "newsletter-subscriptions" (champs: email, createdAt)
#   → Utilisée par le formulaire newsletter du footer
# - Collection "newsletter-subscriptions" doit avoir l'action "create" publique
# ────────────────────────────────────────────────
